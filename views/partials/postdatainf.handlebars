<div class="row medium-8 large-7 columns">
    <div class="blog-post">
       <a href="/post/{{id}}">
    <h3>{{post.title}} <small>{{format_date date_created}}</small></h3>
    <p> {{post.content}} </p>
    <div class="callout">
        <ul class="menu simple">
            <li>Author: {{user.name}} </li>
            {{!-- <li>comments<a href="/post/{{id}}">{{comments.length}} {{format_plural "comment" comments.length}}</a>
                --}}
            <li><a href="/posts/{{id}}">{{comments.length}} {{ comments.length}}</a>
            </li>
        </ul>
    </div></a>
        
    </div>