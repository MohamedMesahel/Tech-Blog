<div class="comments">
    {{#each this}}
    <section class="comment">
        <div class="meta">
            {{user.name}} on {{format_date date_created}}
        </div>
        <div class="text">
            {{content}}
        </div>
    </section>
    {{/each}}
</div>